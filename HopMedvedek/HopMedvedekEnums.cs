namespace HopMedvedek;

public enum EntityTypes { 
    BEAR,
    CROW,
    OWL,
    LAST
}

public enum TrunkTypes { 
    BASE,
    MID,
    LAST
}
public enum BranchTypes
{
    LONG,
    MED,
    SHORT,
    LAST
}
public enum LeafTypes
{ 
    ONE,
    TWO,
    THREE,
    LAST
}
public enum ObjectTypes
{ 
    BED,
    DOOR,
    BOOKSHELF,
    CHAIR,
    POTS,
    PLATE,
    END
}
public enum ResourceStrings
{ 
    BEAR,
    CROW,
    OWL,
    NATURE,
    SKY,
    SPEECH,
    LAST
}
